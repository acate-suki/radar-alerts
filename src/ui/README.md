# ui

Vanilla JS static pages → GitHub Pages (public, no auth). Visualizes what was collected:

- `markets/` — movers (below/above thresholds).
- `news/` — brazil (crimes · politics · health) + tech (ai · hardware).
- `index.html` — home.
